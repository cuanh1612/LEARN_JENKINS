pipeline {
    stages {
        stage('clone') { 
            steps {
                echo 'clone git ..'
                git branch: 'main', url: 'https://github.com/cuanh1612/LEARN_JENKINS.git'
            }
        }
    }
}